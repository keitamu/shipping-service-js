apiVersion: v1
kind: Service
metadata:
  name: product-service-svc
  labels:
    app: product-service
spec:
  type: NodePort
  ports:
    - name: app-port
      targetPort: app-port
      port: 3000
  selector:
    app: product-service
